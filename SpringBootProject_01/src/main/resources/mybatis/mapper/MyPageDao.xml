<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.springboot.jdbc.IMemberDao">
    <update id="editDao">
    	UPDATE member
    	SET name=#{name}, 
    		phoneNum=#{phoneNum}, 
    		email=#{email}, 
    		postcode=#{postcode}, 
    		address=#{address}, 
    		detailaddress=#{detailaddress}
    	WHERE id=#{id}
    </update>
    <delete id="deleteDao">
    	DELETE FROM member WHERE id=#{id}
    </delete>
    
    
</mapper>