<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.springboot.jdbc.IDao">

	<insert id="joinDao" parameterType="com.project.springboot.jdbc.UserDTO">
		insert into member (id, pass, name, phoneNum, email, postcode, address, detailaddress, regidate, isAdmin)
			values (#{id}, #{pass}, #{name}, #{phoneNum}, #{email}, #{postcode}, #{address}, #{detailaddress}, #{regidate}, #{isAdmin})
	</insert>
	
	    <select id="idCheck" resultType="int">
        select count(*) from member where id = #{id}
    </select>
    
	<select id="findByUsername" resultType="com.project.springboot.jdbc.UserDTO">
	select * from member where id = #{id}
	</select>
    
    
</mapper>